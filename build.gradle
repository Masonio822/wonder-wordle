plugins {
    id("java")
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com/masonio/Main'
        )
    }
}

group = "com.masonio"
version = "1.1.0"

repositories {
    mavenCentral()
}

dependencies {
    testImplementation(platform("org.junit:junit-bom:5.10.0"))
    testImplementation("org.junit.jupiter:junit-jupiter")
}

tasks.test {
    useJUnitPlatform()
}